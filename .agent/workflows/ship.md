---
description: Deploy the latest changes to Vercel via GitHub
---

1. Ask the user for a brief description of the changes (commit message).
2. Run the deployment script: `scripts/ship.sh "USER_MESSAGE"` in the `Fortify` directory.
   - *Note: Replace "USER_MESSAGE" with the actual text provided by the user.*
3. Notify the user when the push is complete and remind them to check the Vercel dashboard.
